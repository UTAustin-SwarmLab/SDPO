# Consolidated SDPO dependencies for Linux x86_64 (conda env, README Option 2).
#
# Install order:
#   1. conda create -n sdql python==3.12 -y && conda activate sdql
#   2. pip install -r requirements.txt
#   3. pip install -e .
#   4. pip install flash-attn --no-build-isolation
#   5. (Optional) pip install -r requirements_vllm.txt   # inference stack
#
# Note: torch is pinned here for reproducibility. To match a specific CUDA
# build, install torch from the appropriate index URL BEFORE running
# `pip install -r requirements.txt` (see README Option 2).

# Core training stack
accelerate==1.12.0
codetiming==1.4.0
datasets
dill==0.4.0
hydra-core==1.3.2
# numpy<2.0.0
pandas==2.3.3
peft==0.18.0
pyarrow==22.0.0
pybind11==3.0.1
pylatexenc==2.10
ray[default]==2.53.0
tensordict==0.10.0
transformers==4.57.1
wandb

# Model/runtime
torch==2.10.0
torchdata>=0.10.0
torchvision>=0.24.0
vllm==0.17.0

# Serving and tooling
fastapi==0.127.0
uvicorn
tensorboard
packaging==25.0
psutil==7.1.3
pyzmq==27.1.0

# Math/reasoning utilities used in SDPO tasks
latex2sympy2==1.5.4
latex2sympy2_extended==1.10.2
math-verify
word2number

# Provider SDKs
openai==2.14.0
anthropic==0.71.0
matplotlib>=3.10
jeepney
secretstorage
contourpy>1.2.0
